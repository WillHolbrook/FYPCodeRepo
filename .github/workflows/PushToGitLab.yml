name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  push-to-github:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git pull https://github.com/${{env.GITHUB_REPOSITORY}}.git
      - run: ls -l
      - run: pwd
