name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
#   push:
#     branches: [ develop ]
#   pull_request:
#     branches: [ develop ]
jobs:
  push-to-github:
    runs-on: ubuntu-latest
    steps:    
      - uses: actions/checkout@v3
      - run: cat .gitignore
      - uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{secrets.GITLAB_FYP_CODE_REPO_KEY}}
#       - run: git clone git@github.com:WillHolbrook/FYPCodeRepo.git
#       - run: tree
# #       - run:
# #       - run: cd ./FYPCodeRepo/FYPCodeRepo
      - run: git checkout -B "develop"
      - run: git remote add gitlab git@git.cs.bham.ac.uk:projects-2022-23/wdh961.git
      - run: tree ~
      - run: cat ~/.ssh/config
#       - run: git push gitlab develop
      
#         working-directory: FYPCodeRepo
#       - run: ls -l
#         working-directory: FYPCodeRepo
#       - run: cat .gitignore
#         working-directory: FYPCodeRepo
      
