name: ESLint

on:
  push:
    branches: [ develop ]
#    paths:
#      - backend/**
#      - compose/backend/**
  pull_request:
    branches: [ develop ]
#    paths:
#      - backend/**
#      - compose/backend/**

jobs:
  javascript-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    strategy:
      fail-fast: false
    steps:
    - uses: actions/checkout@v3
    - name: Install dependencies
      run: |
        npm install
        npm install --dev

    - name: Check Styling with Prettier
      run : |
        npx prettier --check .

    - name: Lint with ESLint
      run: |
        npx eslint $(git ls-files -- '*.js') --max-warnings=0 --plugin eslint-plugin-react@7.32.2
